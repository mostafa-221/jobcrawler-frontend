import { ConvertStringToDotsPipe } from './convert-string-to-dots.pipe';

describe('ConvertStringToDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStringToDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
